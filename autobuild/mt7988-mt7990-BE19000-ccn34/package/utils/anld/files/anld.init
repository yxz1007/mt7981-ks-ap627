#!/bin/sh /etc/rc.common

START=80
STOP=90

USE_PROCD=1

start_service() {
	procd_open_instance
	procd_set_param command anld
	procd_set_param respawn
	procd_close_instance
}

stop_service() {
	pid=`ps | grep anld | awk '{print $1}' | sed -n 1p`
	if [ $pid -ne 0 ]
	then
	        kill -9 $pid
	fi
}
